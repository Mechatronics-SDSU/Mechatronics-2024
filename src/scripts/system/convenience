echo "----------------------------------------------------------------------------------- \n"

echo "\nAdding Convenience Scripts to Your linux Commands \n"

# SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

cd src/scripts/system

sudo cp * /usr/local/bin

ls /usr/local/bin

echo "\n\nScripts added!